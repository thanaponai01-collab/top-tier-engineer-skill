Product wants a "download as CSV" button on the reports page in `fixture/`, plus a short
history so a user can see the last few exports they ran — a timestamp and the report title
is enough for launch.

Run `arch-design` in design mode over `fixture/` and tell us how to structure this before
anyone writes code.
