{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then python3 \"${CLAUDE_PLUGIN_ROOT}/tools/stop-gate.py\"; else python \"${CLAUDE_PLUGIN_ROOT}/tools/stop-gate.py\"; fi"
          }
        ]
      }
    ]
  }
}
